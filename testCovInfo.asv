function testCovInfo()
clc;
clear;
R = rodrigues([1 2 3]);
J = rand(8,3);
H = J' * J;

% inv((J*R') * (J*R')') - R * inv(H) * R'

inv((J*R')' * (J*R')) - R * inv(H) * R'
J_other = rand(3,3);
inv(J_other * H * inv(J_other)) - J_other * inv(H) * inv(J_other)

inv(inv(J_other) * H * (J_other)) - inv(J_other) * inv(H) * (J_other)

% (J * eye(3) * J')
inv((J_ * H * J_other)) - (J_other) * inv(H) * J_other'
end